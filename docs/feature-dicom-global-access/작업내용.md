# DICOM 전체 데이터 조회 권한 기능 작업 내용

## 📋 작업 개요

**목적**: 시스템 관리자나 특정 권한을 가진 사용자가 프로젝트 제약 없이 전체 DICOM 데이터를 조회할 수 있도록 함

**주요 변경사항**:
1. `project_id` 파라미터를 옵셔널로 변경
2. DICOM 전체 접근 권한 추가 (Permission, Capability)
3. 권한 기반 데이터 조회 로직 구현

---

## 🗄️ Database Migration

### 파일: `pacs-server/migrations/026_add_dicom_global_access.sql`

#### 1. Permission 추가

```sql
INSERT INTO security_permission (category, resource_type, action) VALUES
    ('DICOM 데이터 관리', 'DICOM', 'READ_ALL');
```

**의미**: 모든 프로젝트의 DICOM 데이터를 조회할 수 있는 권한

#### 2. Capability 추가

```sql
INSERT INTO security_capability (name, display_name, display_label, description, category, category_label) VALUES
    ('DICOM_GLOBAL_ACCESS', 'DICOM 전체 접근', 'Global', '모든 프로젝트의 DICOM 데이터 조회 권한', 'DICOM 데이터 관리', 'DICOM');
```

**Capability 정보**:
- `name`: `DICOM_GLOBAL_ACCESS`
- `display_name`: "DICOM 전체 접근"
- `display_label`: "Global"
- `category`: "DICOM 데이터 관리"

#### 3. Capability-Permission 매핑

```sql
INSERT INTO security_capability_mapping (capability_id, permission_id)
SELECT c.id, p.id
FROM security_capability c, security_permission p
WHERE c.name = 'DICOM_GLOBAL_ACCESS' 
  AND p.resource_type = 'DICOM' 
  AND p.action = 'READ_ALL';
```

#### 4. Role에 Capability 할당

```sql
-- SUPER_ADMIN Role에 할당
INSERT INTO security_role_capability (role_id, capability_id)
SELECT r.id, c.id
FROM security_role r, security_capability c
WHERE r.name = 'SUPER_ADMIN' 
  AND c.name = 'DICOM_GLOBAL_ACCESS';

-- ADMIN Role에 할당
INSERT INTO security_role_capability (role_id, capability_id)
SELECT r.id, c.id
FROM security_role r, security_capability c
WHERE r.name = 'ADMIN' 
  AND c.name = 'DICOM_GLOBAL_ACCESS';
```

**할당된 Role**:
- ✅ `SUPER_ADMIN`
- ✅ `ADMIN`

---

## 🔧 Backend 구현

### 파일: `pacs-server/src/presentation/controllers/dicom_gateway_controller.rs`

#### 1. Import 추가

```rust
use crate::domain::repositories::{AccessConditionRepository, ProjectDataRepository, UserRepository};
use crate::infrastructure::repositories::{AccessConditionRepositoryImpl, ProjectDataRepositoryImpl, UserRepositoryImpl};
```

#### 2. 권한 확인 함수 추가

```rust
/// 사용자가 DICOM 전체 접근 권한을 가지고 있는지 확인
async fn has_global_dicom_access(user_id: i32, pool: &sqlx::PgPool) -> bool {
    sqlx::query_scalar::<_, bool>(
        "SELECT EXISTS(
            SELECT 1 FROM security_user_project sup
            INNER JOIN security_role r ON sup.role_id = r.id
            INNER JOIN security_role_capability src ON r.id = src.role_id
            INNER JOIN security_capability c ON src.capability_id = c.id
            WHERE sup.user_id = $1 
              AND c.name = 'DICOM_GLOBAL_ACCESS'
        )"
    )
    .bind(user_id)
    .fetch_one(pool)
    .await
    .unwrap_or(false)
}
```

**동작**:
1. 사용자의 프로젝트 멤버십 조회
2. 프로젝트의 Role 확인
3. Role에 `DICOM_GLOBAL_ACCESS` Capability가 있는지 확인
4. 하나라도 있으면 `true` 반환

#### 3. `get_studies` 핸들러 수정

**변경 전**:
```rust
pub async fn get_studies(
    qido: web::Data<Dcm4cheeQidoClient>,
    evaluator: web::Data<Arc<DicomRbacEvaluatorImpl>>,
    jwt: web::Data<Arc<JwtService>>,
    access_condition_repo: web::Data<Arc<AccessConditionRepositoryImpl>>,
    user_repo: web::Data<Arc<UserRepositoryImpl>>,
    query: web::Query<GatewayQuery>,
    req: HttpRequest,
) -> HttpResponse {
    // project_id 필수 검증
    let project_id = match query.project_id {
        Some(id) if id > 0 => id,
        _ => {
            return HttpResponse::BadRequest().json(serde_json::json!({
                "error": "project_id is required and must be greater than 0"
            }));
        }
    };
    // ...
}
```

**변경 후**:
```rust
pub async fn get_studies(
    qido: web::Data<Dcm4cheeQidoClient>,
    evaluator: web::Data<Arc<DicomRbacEvaluatorImpl>>,
    jwt: web::Data<Arc<JwtService>>,
    access_condition_repo: web::Data<Arc<AccessConditionRepositoryImpl>>,
    user_repo: web::Data<Arc<UserRepositoryImpl>>,
    project_data_repo: web::Data<Arc<ProjectDataRepositoryImpl>>,  // ← 추가
    query: web::Query<GatewayQuery>,
    req: HttpRequest,
) -> HttpResponse {
    // 사용자 ID 추출
    let user_id = match extract_user_id_from_request(&req, &jwt, &user_repo).await {
        Some(id) if id > 0 => id,
        _ => {
            return HttpResponse::Unauthorized().json(serde_json::json!({
                "error": "Invalid or missing authorization token"
            }));
        }
    };

    // 전체 데이터 조회 권한 확인
    let has_global_access = has_global_dicom_access(user_id, project_data_repo.pool()).await;

    // 프로젝트 ID 검증
    let project_id_opt = query.project_id;
    
    // 전체 데이터 조회 권한이 없으면 project_id 필수
    if !has_global_access && project_id_opt.is_none() {
        return HttpResponse::BadRequest().json(serde_json::json!({
            "error": "project_id is required (no global access permission)"
        }));
    }
    
    // project_id가 있으면 검증
    if let Some(id) = project_id_opt {
        if id <= 0 {
            return HttpResponse::BadRequest().json(serde_json::json!({
                "error": "project_id must be greater than 0"
            }));
        }
    }
    
    // Access Condition은 project_id가 있을 때만 적용
    let qido_params = if let Some(pid) = project_id_opt {
        if let Ok(conditions) = access_condition_repo.list_by_project(pid).await {
            let rule_params = build_qido_params_from_conditions(&conditions);
            merge_qido_params(rule_params, user_params)
        } else {
            user_params
        }
    } else {
        user_params
    };
    
    // RBAC 필터링 적용
    let filtered = if has_global_access && project_id_opt.is_none() {
        // 전체 데이터 조회 권한이 있고 project_id가 없으면 필터링 안 함
        qido_response
    } else if let Some(pid) = project_id_opt {
        // project_id가 있으면 RBAC 필터링 적용
        // ... (기존 필터링 로직)
    } else {
        qido_response
    };
    // ...
}
```

**주요 변경점**:
1. ✅ `project_data_repo` 파라미터 추가
2. ✅ 사용자 ID 추출을 먼저 수행
3. ✅ `has_global_dicom_access()` 호출하여 권한 확인
4. ✅ `project_id`를 옵셔널로 처리
5. ✅ 전체 권한이 없으면 `project_id` 필수
6. ✅ Access Condition은 `project_id`가 있을 때만 적용
7. ✅ RBAC 필터링은 전체 권한이 있고 `project_id`가 없으면 스킵

#### 4. `get_series` 핸들러 수정

`get_studies`와 동일한 로직 적용:
- ✅ `project_data_repo` 파라미터 추가
- ✅ 권한 확인 로직 추가
- ✅ `project_id` 옵셔널 처리
- ✅ Access Condition 조건부 적용
- ✅ RBAC 필터링 조건부 적용

#### 5. `get_instances` 핸들러 수정

`get_studies`, `get_series`와 동일한 로직 적용

---

## 📝 API 사용 예시

### 시나리오 1: 전체 데이터 조회 (SUPER_ADMIN)

**요청**:
```bash
GET /api/dicom/studies
Authorization: Bearer <SUPER_ADMIN_TOKEN>
```

**동작**:
1. ✅ 사용자 인증 (user_id 추출)
2. ✅ 전체 데이터 조회 권한 확인 → `true`
3. ✅ `project_id` 없음 → 허용
4. ✅ Access Condition 적용 안 함
5. ✅ RBAC 필터링 안 함
6. ✅ 모든 DICOM Study 반환

**응답**:
```json
[
  {
    "0020000D": {"Value": ["1.2.826.0.1.3680043.8.498.11111111111111111111111111111111"]},
    "00100020": {"Value": ["PATIENT001"]},
    "00080060": {"Value": ["CT"]}
  },
  {
    "0020000D": {"Value": ["1.2.826.0.1.3680043.8.498.22222222222222222222222222222222"]},
    "00100020": {"Value": ["PATIENT002"]},
    "00080060": {"Value": ["MR"]}
  },
  ...
]
```

### 시나리오 2: 프로젝트별 조회 (SUPER_ADMIN)

**요청**:
```bash
GET /api/dicom/studies?project_id=150
Authorization: Bearer <SUPER_ADMIN_TOKEN>
```

**동작**:
1. ✅ 사용자 인증
2. ✅ 전체 데이터 조회 권한 확인 → `true`
3. ✅ `project_id=150` 제공됨
4. ✅ Access Condition 적용 (프로젝트 150의 필터링 규칙)
5. ✅ RBAC 필터링 적용
6. ✅ 프로젝트 150의 필터링된 데이터 반환

**응답**: 프로젝트 150의 Access Condition에 맞는 데이터만 반환

### 시나리오 3: 전체 데이터 조회 시도 (일반 사용자)

**요청**:
```bash
GET /api/dicom/studies
Authorization: Bearer <USER_TOKEN>
```

**동작**:
1. ✅ 사용자 인증
2. ✅ 전체 데이터 조회 권한 확인 → `false`
3. ❌ `project_id` 없음 → 거부
4. ❌ 400 Bad Request 반환

**응답**:
```json
{
  "error": "project_id is required (no global access permission)"
}
```

### 시나리오 4: 프로젝트별 조회 (일반 사용자, 기존 동작)

**요청**:
```bash
GET /api/dicom/studies?project_id=150
Authorization: Bearer <USER_TOKEN>
```

**동작**:
1. ✅ 사용자 인증
2. ✅ 전체 데이터 조회 권한 확인 → `false`
3. ✅ `project_id=150` 제공됨
4. ✅ Access Condition 적용
5. ✅ RBAC 필터링 적용
6. ✅ 프로젝트 150의 필터링된 데이터 반환 (기존과 동일)

**응답**: 기존과 동일

---

## ✅ 검증 결과

### 1. Migration 검증

```sql
-- Capability 확인
SELECT 
    c.name as capability_name,
    c.display_name,
    p.resource_type,
    p.action
FROM security_capability c
LEFT JOIN security_capability_mapping cm ON c.id = cm.capability_id
LEFT JOIN security_permission p ON cm.permission_id = p.id
WHERE c.name = 'DICOM_GLOBAL_ACCESS';
```

**결과**:
```
   capability_name   |  display_name   | resource_type |  action  
---------------------+-----------------+---------------+----------
 DICOM_GLOBAL_ACCESS | DICOM 전체 접근 | DICOM         | READ_ALL
```

```sql
-- Role 할당 확인
SELECT 
    r.name as role_name,
    c.name as capability_name,
    c.display_name
FROM security_role r
INNER JOIN security_role_capability src ON r.id = src.role_id
INNER JOIN security_capability c ON src.capability_id = c.id
WHERE c.name = 'DICOM_GLOBAL_ACCESS';
```

**결과**:
```
  role_name  |   capability_name   |  display_name   
-------------+---------------------+-----------------
 SUPER_ADMIN | DICOM_GLOBAL_ACCESS | DICOM 전체 접근
 ADMIN       | DICOM_GLOBAL_ACCESS | DICOM 전체 접근
```

### 2. 컴파일 검증

```bash
cd pacs-server && cargo build
```

**결과**: ✅ 성공 (warnings만 있음, errors 없음)

---

## 📊 변경된 파일 목록

| 파일 | 변경 내용 | 라인 수 |
|------|----------|---------|
| `pacs-server/migrations/026_add_dicom_global_access.sql` | Migration 추가 | +32 (new) |
| `pacs-server/src/presentation/controllers/dicom_gateway_controller.rs` | 권한 확인 및 핸들러 수정 | +150, -69 |
| `docs/feature-dicom-global-access/작업계획.md` | 작업 계획 문서 | +300 (new) |
| **총계** | | **+482, -69** |

---

## 🎯 달성된 목표

- ✅ DICOM 전체 접근 권한 추가 (Permission, Capability)
- ✅ SUPER_ADMIN, ADMIN Role에 권한 할당
- ✅ `project_id` 파라미터 옵셔널화
- ✅ 권한 기반 데이터 조회 로직 구현
- ✅ 하위 호환성 100% 유지
- ✅ 컴파일 성공

---

## ✅ 테스트 작성 완료

### 1. 단위 테스트

**파일**: `pacs-server/tests/dicom_gateway_global_access_unit_test.rs`

**테스트 케이스**:
1. ✅ `test_has_global_dicom_access_super_admin` - SUPER_ADMIN 권한 확인
2. ✅ `test_has_global_dicom_access_admin` - ADMIN 권한 확인
3. ✅ `test_has_global_dicom_access_regular_user` - 일반 사용자 권한 없음
4. ✅ `test_has_global_dicom_access_nonexistent_user` - 존재하지 않는 사용자
5. ✅ `test_has_global_dicom_access_project_admin` - PROJECT_ADMIN 권한 없음
6. ✅ `test_has_global_dicom_access_viewer` - VIEWER 권한 없음

### 2. 통합 테스트

**파일**: `pacs-server/tests/dicom_gateway_global_access_integration_test.rs`

**테스트 케이스** (8개 모두 통과):
1. ✅ `test_global_access_without_project_id_super_admin` - 전체 데이터 조회 (SUPER_ADMIN)
2. ✅ `test_global_access_with_project_id_super_admin` - 프로젝트별 조회 (SUPER_ADMIN)
3. ✅ `test_no_global_access_without_project_id_regular_user` - 전체 데이터 조회 실패 (일반 사용자)
4. ✅ `test_backward_compatibility_regular_user_with_project_id` - 하위 호환성 (일반 사용자)
5. ✅ `test_invalid_project_id_zero` - 잘못된 project_id (0)
6. ✅ `test_invalid_project_id_negative` - 잘못된 project_id (음수)
7. ✅ `test_series_endpoint_global_access` - Series 엔드포인트 전체 조회
8. ✅ `test_instances_endpoint_global_access` - Instances 엔드포인트 전체 조회

### 3. 대시보드 시나리오 테스트

**파일**: `auth-dashboard/src/components/DicomGlobalAccess.test.tsx`

**테스트 시나리오** (10개 모두 통과):
1. ✅ SUPER_ADMIN - 전체 데이터 조회
2. ✅ SUPER_ADMIN - 프로젝트별 조회
3. ✅ ADMIN - 전체 데이터 조회
4. ✅ ADMIN - 프로젝트별 조회
5. ✅ 일반 사용자 - 전체 데이터 조회 실패
6. ✅ 일반 사용자 - 프로젝트별 조회 (하위 호환성)
7. ✅ SUPER_ADMIN - Series 전체 조회
8. ✅ 일반 사용자 - Series 조회 실패
9. ✅ 에러 처리 - project_id=0
10. ✅ 에러 처리 - project_id=-1

### 4. Jest 캐시 문제 해결

**문제**: 대시보드 테스트에서 disk cache 관련 에러 발생

**해결 방법**:
1. ✅ `package.json`의 test 스크립트에 `--no-cache` 플래그 추가
2. ✅ `setupTests.ts`에 axios mock 추가 (ESM import 문제 해결)

---

## 📊 변경 통계

| 파일 | 변경 내용 | 라인 수 |
|------|----------|---------|
| **Backend** | | |
| `026_add_dicom_global_access.sql` | Migration 추가 | +32 (new) |
| `dicom_gateway_controller.rs` | 권한 확인 및 핸들러 수정 | +150, -69 |
| **Tests** | | |
| `dicom_gateway_global_access_unit_test.rs` | 단위 테스트 | +302 (new) |
| `dicom_gateway_global_access_integration_test.rs` | 통합 테스트 | +300 (new) |
| `DicomGlobalAccess.test.tsx` | 대시보드 시나리오 테스트 | +300 (new) |
| **Dashboard** | | |
| `package.json` | Jest 캐시 비활성화 | +1, -1 |
| `setupTests.ts` | axios mock 추가 | +14 |
| **Documentation** | | |
| `작업계획.md` | 작업 계획 문서 | +300 (new) |
| `작업내용.md` | 작업 내용 문서 | +600 (new) |
| **총계** | | **+1999, -70** |

---

## ⚠️ 주의사항

### 1. 보안
- **전체 데이터 조회 권한은 매우 강력한 권한**
- SUPER_ADMIN, ADMIN Role에만 할당됨
- 권한 확인 로직이 우회되지 않도록 주의

### 2. 성능
- 전체 데이터 조회 시 대량의 데이터가 반환될 수 있음
- QIDO 서버의 `limit` 파라미터 활용 권장
- 페이지네이션 고려 필요

### 3. 하위 호환성
- ✅ 기존 `project_id` 파라미터 동작 100% 유지
- ✅ 기존 API 호출에 영향 없음
- ✅ 일반 사용자는 기존과 동일하게 동작

