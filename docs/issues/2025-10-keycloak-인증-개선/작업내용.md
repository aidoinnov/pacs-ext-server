# 작업 내용: Keycloak 인증 시스템 개선

## 📌 작업 개요

**기간**: 2025년 10월 25일 ~ 11월 13일  
**관련 파일**: `pacs-server/src/infrastructure/external/keycloak_client.rs`  
**주요 목표**: Keycloak 인증 시스템 개선 및 사용자 관리 기능 강화

---

## 🔍 주요 변경사항 요약

### 1. 토큰 갱신 API 구현 (2025-10-25)
- **커밋**: `0941571` - feat: 토큰 갱신 API 구현 완료
- Keycloak refresh token을 사용한 access token 갱신 기능 추가
- Clean Architecture 패턴 준수

### 2. 회원가입 이메일 인증 우회 (2025-10-27)
- **커밋**: `ad4e3d7` - feat: 회원가입 이메일 인증 우회 및 계정 관리 API 구현
- 이메일 인증 없이 관리자 승인 방식으로 변경
- 아이디 찾기 및 비밀번호 재설정 API 추가

### 3. Keycloak Admin 토큰 획득 방식 개선 (2025-10-28)
- **커밋 시리즈**: `9f194e2` ~ `642c146`
- Service account 기반 client credentials 방식으로 변경
- Realm 설정 동적 처리

### 4. 사용자 삭제 기능 개선 (2025-10-28)
- **커밋**: `5e716be` - fix: Service account로 Keycloak 사용자 삭제 구현 완료
- Service account 토큰으로 사용자 삭제 성공
- Username 기반 사용자 삭제 기능 추가

### 5. 회원가입 정책 변경 (2025-10-28)
- **커밋**: `416e71d` - fix: 회원가입 시 Keycloak 사용자 비활성화 상태로 생성
- 사용자를 비활성화 상태로 생성 (관리자 승인 필요)
- 이메일 인증 필수 제거

### 6. 인증 API 프록시 구현 (2025-11-10)
- **커밋**: `81416f7` - feat: enhance auth API to proxy Keycloak authentication
- 클라이언트가 Keycloak과 직접 통신하지 않고 서버를 통해 인증
- Username/password 기반 로그인으로 변경

### 7. 전반적인 리팩토링 (2025-11-13)
- **커밋**: `cf96ef0` - refactor: improve auth and user services
- 코드 품질 개선 및 구조 정리

---

## 🛠️ 상세 변경 내용

### 1. 토큰 갱신 API 구현

#### 추가된 메서드: `refresh_access_token`

```rust
/// Refresh access token using Keycloak's refresh token endpoint
pub async fn refresh_access_token(
    &self,
    refresh_token: &str,
) -> Result<KeycloakTokenResponse, ServiceError> {
    let url = format!(
        "{}/realms/{}/protocol/openid-connect/token",
        self.base_url, self.realm
    );

    let request = RefreshTokenRequest {
        grant_type: "refresh_token".to_string(),
        refresh_token: refresh_token.to_string(),
        client_id: self.client_id.clone(),
        client_secret: self.client_secret.clone(),           
    };

    let response = self
        .http_client
        .post(&url)
        .form(&request)
        .send()
        .await
        .map_err(|e| {
            ServiceError::ExternalServiceError(format!("Refresh token request failed: {}", e))
        })?;

    if !response.status().is_success() {
        let status = response.status();
        let body = response.text().await.unwrap_or_default();
        return Err(ServiceError::ExternalServiceError(format!(
            "Refresh token failed ({}): {}",
            status, body
        )));
    }

    let token_response: KeycloakTokenResponse = response.json().await.map_err(|e| {
        ServiceError::ExternalServiceError(format!(
            "Failed to parse refresh token response: {}",
            e
        ))
    })?;

    Ok(token_response)
}
```

**주요 기능**:
- Refresh token을 사용하여 새로운 access token 발급
- Client credentials 포함하여 보안 강화
- 에러 처리 및 로깅 개선

---

### 2. 비밀번호 재설정 기능

#### 추가된 메서드: `reset_user_password`

```rust
/// 사용자 비밀번호 재설정 (관리자 권한)
pub async fn reset_user_password(
    &self,
    keycloak_user_id: &str,
    new_password: &str,
) -> Result<(), ServiceError> {
    let token = self.get_admin_token().await?;

    let url = format!(
        "{}/admin/realms/{}/users/{}/reset-password",
        self.base_url, self.realm, keycloak_user_id
    );

    let credential = json!({
        "type": "password",
        "value": new_password,
        "temporary": false
    });

    let response = self
        .http_client
        .put(&url)
        .bearer_auth(&token)
        .json(&credential)
        .send()
        .await?;

    if !response.status().is_success() {
        let status = response.status();
        let body = response.text().await.unwrap_or_default();
        return Err(ServiceError::ExternalServiceError(format!(
            "비밀번호 재설정 실패 ({}): {}",
            status, body
        )));
    }

    Ok(())
}
```

**주요 기능**:
- 관리자 권한으로 사용자 비밀번호 재설정
- Temporary 플래그를 false로 설정하여 즉시 사용 가능

---

### 3. Admin 토큰 획득 방식 개선

#### 수정된 메서드: `get_admin_token`

**변경 전**:
```rust
// Username/password 방식 사용
let params = [
    ("grant_type", "password"),
    ("username", &self.admin_username),
    ("password", &self.admin_password),
    ("client_id", &self.client_id),
];
```

**변경 후**:
```rust
// Service account - client credentials 방식 사용
let params = [
    ("grant_type", "client_credentials"),
    ("client_id", &self.client_id),
    ("client_secret", &self.client_secret),
];
```

**변경 이유**:
- Service account 방식이 더 안전하고 권장되는 방식
- Admin 사용자 계정 정보 노출 위험 제거
- Client credentials만으로 관리 작업 수행 가능

**디버깅 로그 추가**:
```rust
eprintln!("DEBUG: Requesting Keycloak token from: {}", url);
eprintln!("DEBUG: Token request response status: {}", response.status());
eprintln!("DEBUG: Token request successful");
```

---

### 4. 사용자 삭제 기능 개선

#### 개선된 메서드: `delete_user`

**추가된 기능**:
```rust
if status == StatusCode::NOT_FOUND {
    // 이미 삭제됨 - 성공으로 간주
    eprintln!("DEBUG: User {} already deleted (404), treating as success", keycloak_user_id);
    return Ok(());
}
```

**주요 개선사항**:
- 404 에러를 성공으로 처리 (멱등성 보장)
- 상세한 디버깅 로그 추가
- 에러 메시지 개선

#### 추가된 메서드: `delete_user_by_username`

```rust
/// 3-1. username으로 사용자 삭제
pub async fn delete_user_by_username(&self, username: &str) -> Result<(), ServiceError> {
    let token = self.get_admin_token().await?;

    // 1. username으로 사용자 검색
    let search_url = format!(
        "{}/admin/realms/{}/users?username={}&exact=true",
        self.base_url, self.realm, username
    );

    eprintln!("DEBUG: Searching Keycloak user by username: {}", username);

    let search_response = self
        .http_client
        .get(&search_url)
        .bearer_auth(&token)
        .send()
        .await
        .map_err(|e| {
            ServiceError::ExternalServiceError(format!("Keycloak search user failed: {}", e))
        })?;

    if !search_response.status().is_success() {
        let status = search_response.status();
        let body = search_response.text().await.unwrap_or_default();
        eprintln!("ERROR: Keycloak search user failed ({}): {}", status, body);
        return Err(ServiceError::ExternalServiceError(format!(
            "Keycloak search user failed ({}): {}",
            status, body
        )));
    }

    let users: Vec<serde_json::Value> = search_response.json().await.map_err(|e| {
        ServiceError::ExternalServiceError(format!("Failed to parse search response: {}", e))
    })?;

    if users.is_empty() {
        eprintln!("DEBUG: User {} not found in Keycloak, treating as success", username);
        return Ok(());
    }

    // 2. 찾은 사용자 삭제
    for user in users {
        if let Some(user_id) = user.get("id").and_then(|v| v.as_str()) {
            eprintln!("DEBUG: Found user {} with ID: {}, deleting...", username, user_id);
            self.delete_user(user_id).await?;
        }
    }

    Ok(())
}
```

**주요 기능**:
- Username으로 사용자 검색 후 삭제
- Exact match 검색으로 정확성 보장
- 사용자가 없을 경우 성공으로 처리 (멱등성)

---

### 5. 회원가입 정책 변경

#### 수정된 메서드: `create_user`

**변경 전**:
```rust
let create_request = CreateUserRequest {
    username: username.to_string(),
    email: email.to_string(),
    enabled: true,              // 사용자 활성화
    email_verified: false,      // 이메일 미인증
    credentials: vec![...],
    required_actions: vec!["VERIFY_EMAIL".to_string()],  // 이메일 인증 필요
};

// 이메일 인증 메일 발송
self.send_verification_email(&token, &user_id).await?;
```

**변경 후**:
```rust
let create_request = CreateUserRequest {
    username: username.to_string(),
    email: email.to_string(),
    enabled: false,       // 사용자는 비활성화 상태로 생성 (관리자 승인 필요)
    email_verified: true, // 이메일은 이미 인증된 것으로 간주
    credentials: vec![Credential {
        credential_type: "password".to_string(),
        value: password.to_string(),
        temporary: false,
    }],
    required_actions: vec![], // 이메일 인증 필요 없음
};

// user 역할 할당
let _ = self.assign_realm_role(&token, &user_id, "user").await;

// 이메일 인증 메일 발송하지 않음 (관리자 승인 방식 사용)
```

**변경 이유**:
- 이메일 인증 프로세스 복잡도 제거
- 관리자 승인 방식으로 보안 강화
- 사용자 경험 개선 (이메일 확인 불필요)

**주요 변경사항**:
- ✅ `enabled: false` - 관리자 승인 필요
- ✅ `email_verified: true` - 이메일 인증 필수 제거
- ✅ `required_actions: []` - 이메일 인증 액션 제거
- ❌ 이메일 인증 메일 발송 제거

---

### 6. 사용자 인증 API 추가

#### 추가된 메서드: `authenticate_user`

```rust
/// Authenticate user with username and password
pub async fn authenticate_user(
    &self,
    username: &str,
    password: &str,
) -> Result<KeycloakTokenResponse, ServiceError> {
    let url = format!(
        "{}/realms/{}/protocol/openid-connect/token",
        self.base_url, self.realm
    );

    let params = [
        ("grant_type", "password"),
        ("client_id", &self.client_id),
        ("client_secret", &self.client_secret),
        ("username", username),
        ("password", password),
    ];

    let response = self
        .http_client
        .post(&url)
        .form(&params)
        .send()
        .await
        .map_err(|e| {
            ServiceError::ExternalServiceError(format!("Authentication request failed: {}", e))
        })?;

    if !response.status().is_success() {
        let status = response.status();
        let body = response.text().await.unwrap_or_default();
        return Err(ServiceError::Unauthorized(format!(
            "Authentication failed ({}): {}",
            status, body
        )));
    }

    let token_response: KeycloakTokenResponse = response.json().await.map_err(|e| {
        ServiceError::ExternalServiceError(format!(
            "Failed to parse authentication response: {}",
            e
        ))
    })?;

    Ok(token_response)
}
```

**주요 기능**:
- Username/password로 Keycloak 인증
- Access token 및 refresh token 반환
- 클라이언트가 Keycloak과 직접 통신하지 않도록 프록시 역할

---

## 📊 영향 범위

### 변경된 파일
1. `pacs-server/src/infrastructure/external/keycloak_client.rs` (주요 변경)
2. `pacs-server/src/domain/services/auth_service.rs`
3. `pacs-server/src/application/use_cases/auth_use_case.rs`
4. `pacs-server/src/application/dto/auth_dto.rs`
5. `pacs-server/src/presentation/controllers/auth_controller.rs`

### 추가된 기능
1. ✅ 토큰 갱신 API (`/api/auth/refresh`)
2. ✅ 비밀번호 재설정 API
3. ✅ Username 기반 사용자 삭제
4. ✅ 사용자 인증 프록시 API
5. ✅ 관리자 승인 기반 회원가입

### 변경된 동작
1. 🔄 Admin 토큰 획득: Password grant → Client credentials
2. 🔄 회원가입: 이메일 인증 필수 → 관리자 승인 필요
3. 🔄 로그인: Keycloak ID → Username/password
4. 🔄 사용자 삭제: 404 에러 → 성공으로 처리

---

## 🧪 테스트 결과

### 단위 테스트
```bash
# 토큰 갱신 테스트
pacs-server/tests/keycloak_client_refresh_token_test.rs
pacs-server/tests/auth_service_refresh_token_test.rs
pacs-server/tests/auth_use_case_refresh_token_test.rs
pacs-server/tests/auth_controller_refresh_token_test.rs
```

### 통합 테스트
```bash
pacs-server/tests/refresh_token_integration_test.rs
```

### 성능 테스트
```bash
pacs-server/tests/refresh_token_performance_test.rs
```

---

## ✅ 완료 항목

- [x] 토큰 갱신 API 구현
- [x] 비밀번호 재설정 API 구현
- [x] Admin 토큰 획득 방식 개선 (Client credentials)
- [x] 사용자 삭제 기능 개선 (멱등성 보장)
- [x] Username 기반 사용자 삭제 추가
- [x] 회원가입 정책 변경 (관리자 승인 방식)
- [x] 사용자 인증 프록시 API 구현
- [x] 디버깅 로그 추가
- [x] 에러 처리 개선
- [x] 단위/통합/성능 테스트 작성
- [x] API 문서화 (`docs/api/AUTH_API.md`)

---

## 🎯 주요 개선 효과

### 1. 보안 강화
- Service account 기반 인증으로 관리자 계정 노출 위험 제거
- Client credentials 방식으로 보안 수준 향상
- 관리자 승인 방식으로 무분별한 회원가입 방지

### 2. 사용자 경험 개선
- 이메일 인증 단계 제거로 회원가입 프로세스 간소화
- 토큰 갱신으로 재로그인 없이 세션 유지
- 비밀번호 재설정 기능으로 계정 복구 용이

### 3. 시스템 안정성 향상
- 멱등성 보장으로 중복 요청 처리 개선
- 상세한 로깅으로 디버깅 용이성 증가
- 에러 처리 개선으로 예외 상황 대응 강화

### 4. 아키텍처 개선
- Clean Architecture 패턴 준수
- 클라이언트가 Keycloak과 직접 통신하지 않도록 프록시 구조
- 계층별 책임 분리 명확화

---

## 📝 관련 문서

- `docs/api/AUTH_API.md` - 인증 API 상세 문서
- `work/token_refresh_api/01_work_plan.md` - 토큰 갱신 작업 계획
- `work/token_refresh_api/02_work_completion.md` - 토큰 갱신 작업 완료 보고
- `work/token_refresh_api/03_technical_document.md` - 토큰 갱신 기술 문서
- `work/email_verification_bypass/work_plan.md` - 이메일 인증 우회 작업 계획

---

## 🔄 마이그레이션 가이드

### 클라이언트 측 변경사항

#### 1. 로그인 API 변경
```javascript
// ❌ 이전 방식
POST /api/auth/login
{
  "keycloak_id": "user-uuid-123"
}

// ✅ 새로운 방식
POST /api/auth/login
{
  "username": "user@example.com",
  "password": "password123"
}
```

#### 2. 토큰 갱신 API 사용
```javascript
// ✅ 새로운 기능
POST /api/auth/refresh
{
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}

// 응답
{
  "access_token": "new-access-token",
  "refresh_token": "new-refresh-token",
  "expires_in": 300,
  "refresh_expires_in": 1800,
  "token_type": "Bearer"
}
```

#### 3. 회원가입 후 처리
```javascript
// ✅ 회원가입 성공 후
{
  "message": "회원가입이 완료되었습니다. 관리자 승인 후 로그인이 가능합니다."
}

// 사용자에게 안내
// "회원가입이 완료되었습니다. 관리자 승인 후 이용 가능합니다."
```

### 환경 변수 설정

```bash
# Keycloak 설정
KEYCLOAK_URL=https://keycloak.example.com
KEYCLOAK_REALM=dcm4che
KEYCLOAK_CLIENT_ID=pacs-server
KEYCLOAK_CLIENT_SECRET=your-client-secret

# Admin 계정 정보 (더 이상 사용하지 않음, 하위 호환성 유지)
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=admin
```

---

## 🚀 배포 체크리스트

- [x] 코드 변경 완료
- [x] 단위 테스트 통과
- [x] 통합 테스트 통과
- [x] 성능 테스트 통과
- [x] API 문서 업데이트
- [x] CHANGELOG 업데이트
- [ ] 클라이언트 팀에 변경사항 공유
- [ ] 환경 변수 설정 확인
- [ ] Keycloak 설정 확인 (Service account 활성화)
- [ ] 프로덕션 배포
- [ ] 모니터링 및 로그 확인

