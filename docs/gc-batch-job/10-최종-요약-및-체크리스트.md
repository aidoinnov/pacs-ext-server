# 10. 최종 요약 및 체크리스트

> **목적**: GC Batch Job 구현 전체 요약 및 실행 가능한 체크리스트
>
> **상태**: ✅ **Phase 1 구현 완료** (2026-01-13)

---

## 🎉 구현 완료 요약

### ✅ 완료된 작업
1. **ObjectStorageService 추상화 계층** - Trait 기반 S3 연동
2. **GC Service Layer** - Job A/B 비즈니스 로직 (트랜잭션 일관성 개선)
3. **Repository Layer** - DB 쿼리 및 로그 기록
4. **Binary Layer** - CLI 인터페이스 (`gc_runner`) + Advisory Lock
5. **Database Migration** - snapshot 컬럼 및 GC 로그 테이블 (file_size 포함)
6. **E2E 테스트** - 14개 시나리오 모두 통과
7. **file_size 기록** - S3 파일 크기 추적 기능
8. **동시 실행 방지** - PostgreSQL Advisory Lock

### 🚀 실행 가능 상태
- ✅ 빌드 성공
- ✅ CLI 실행 가능
- ✅ Dry-run 모드 테스트 완료
- ✅ 데이터베이스 마이그레이션 완료
- ✅ E2E 테스트 14개 시나리오 통과
- ✅ Advisory Lock 동시 실행 방지 검증 완료

### 🆕 주요 개선사항 (2026-01-13)
1. **트랜잭션 일관성** - 로그 기록 실패 시에도 GC 작업 계속 진행
2. **멱등성 보장** - 중복 실행 시 안전하게 처리
3. **Grace Period 경계값** - 정확히 일치하는 경우도 처리
4. **빈 결과 처리** - 처리할 항목 없어도 정상 종료
5. **Batch Size 경계값** - 정확한 배치 크기 준수
6. **NULL 처리** - snapshot_image_key NULL 자동 필터링
7. **동시 실행 방지** - Job A (1001), Job B (1002) 독립적 락
8. **file_size 기록** - 용량 모니터링 및 비용 분석

---

## 📊 프로젝트 개요

### 목표
- **PENDING 상태 타임아웃 처리** (Job A) - 3일 이상 pending → failed
- **FAILED 상태 스냅샷 정리** (Job B) - 7일 이상 failed → S3 삭제

### 아키텍처
```
gc_runner (Binary)
    ↓
GcService (Business Logic)
    ↓
GcRepository + GcLogRepository (DB Query)
    ↓
ObjectStorageService (S3 Abstraction)
    ↓
PostgreSQL + S3
```

### 핵심 설계 원칙
- ✅ 단일 책임 원칙 (SRP)
- ✅ 계층 분리 (Domain, Repository, Service, Binary)
- ✅ 의존성 주입 (DI)
- ✅ Trait 기반 추상화 (ObjectStorageService)
- ✅ 에러 처리 일관성
- ✅ 기존 코드베이스 패턴 준수

---

## 📁 파일 구조

```
pacs-server/
├── migrations/
│   └── 039_create_gc_deletion_log.sql          # DB 마이그레이션
│
├── src/
│   ├── domain/
│   │   ├── entities/
│   │   │   ├── gc_deletion_log.rs              # GC 로그 Entity
│   │   │   └── mod.rs                          # ✅ 업데이트 필요
│   │   └── repositories/
│   │       ├── gc_repository.rs                # GC Repository Trait
│   │       ├── gc_log_repository.rs            # GC Log Repository Trait
│   │       └── mod.rs                          # ✅ 업데이트 필요
│   │
│   ├── infrastructure/
│   │   └── repositories/
│   │       ├── gc_repository_impl.rs           # GC Repository 구현
│   │       ├── gc_log_repository_impl.rs       # GC Log Repository 구현
│   │       └── mod.rs                          # ✅ 업데이트 필요
│   │
│   ├── application/
│   │   └── services/
│   │       ├── gc_service.rs                   # GC Service Trait
│   │       ├── gc_service_impl.rs              # GC Service 구현
│   │       └── mod.rs                          # ✅ 업데이트 필요
│   │
│   └── bin/
│       └── gc_runner.rs                        # CLI Binary
│
├── tests/
│   ├── migrations/
│   │   ├── test_039_gc_deletion_log.sh         # 마이그레이션 테스트
│   │   └── test_gc_log_insert.sql              # 데이터 삽입 테스트
│   ├── domain/
│   │   └── gc_deletion_log_test.rs             # Entity 테스트
│   ├── repositories/
│   │   ├── gc_repository_test.rs               # GC Repository 테스트
│   │   └── gc_log_repository_test.rs           # GC Log Repository 테스트
│   ├── services/
│   │   └── gc_service_test.rs                  # Service 테스트 (Mock)
│   ├── integration/
│   │   └── gc_service_integration_test.rs      # 통합 테스트
│   ├── bin/
│   │   ├── gc_runner_cli_test.rs               # CLI 테스트
│   │   ├── test_gc_runner.sh                   # Binary 실행 테스트
│   │   └── test_env_validation.sh              # 환경 변수 테스트
│   └── e2e/
│       ├── docker-compose.test.yml             # E2E 환경 구성
│       ├── test_gc_e2e.py                      # E2E Python 시나리오
│       └── run_e2e_tests.sh                    # E2E 실행 스크립트
│
├── k8s/
│   └── cronjobs/
│       ├── gc-job-a-timeout-pending.yaml       # Job A CronJob
│       └── gc-job-b-cleanup-failed.yaml        # Job B CronJob
│
├── Cargo.toml                                   # ✅ 업데이트 필요
└── Dockerfile                                   # ✅ 업데이트 필요
```

---

## ✅ 구현 체크리스트

### Phase 0: ObjectStorageService 추상화 (완료 ✅)
- [x] `src/domain/services/object_storage_service.rs` 작성
  - [x] `ObjectStorageService` trait 정의
  - [x] `ObjectStorageError` 에러 타입
- [x] `src/infrastructure/services/s3_object_storage_service.rs` 작성
  - [x] S3 구현체
  - [x] 파일 업로드/삭제/메타데이터 조회
- [x] `src/infrastructure/services/object_storage_service_factory.rs` 작성
  - [x] Factory 패턴 구현
- [x] `src/domain/services/mod.rs` 업데이트
- [x] `src/infrastructure/services/mod.rs` 업데이트

### Phase 1: DB 마이그레이션 (완료 ✅)
- [x] `migrations/036_add_snapshot_image_to_annotations.sql` 작성
  - [x] `snapshot_upload_status` ENUM 타입
  - [x] `snapshot_image_key`, `snapshot_status`, `snapshot_uploaded_at` 컬럼
  - [x] 인덱스 생성
- [x] `migrations/039_create_gc_deletion_log.sql` 작성
  - [x] `gc_deletion_log` 테이블
  - [x] 외래 키 제약 조건
  - [x] 인덱스 생성
- [x] 로컬 DB에 마이그레이션 실행
- [x] 테이블 생성 확인

### Phase 2: Domain Layer (완료 ✅)
- [x] `src/domain/entities/gc_deletion_log.rs` 작성
  - [x] `GcDeletionLog` struct
  - [x] `NewGcDeletionLog` struct
- [x] `src/domain/entities/mod.rs` 업데이트
- [x] `src/domain/repositories/gc_repository.rs` 작성
  - [x] `GcRepository` trait 정의
- [x] `src/domain/repositories/gc_log_repository.rs` 작성
  - [x] `GcLogRepository` trait 정의
- [x] `src/domain/repositories/mod.rs` 업데이트

### Phase 3: Repository Layer (완료 ✅)
- [x] `src/infrastructure/repositories/gc_repository_impl.rs` 작성
  - [x] `find_pending_snapshots()` 구현
  - [x] `find_failed_snapshots()` 구현
  - [x] `update_snapshot_status()` 구현
- [x] `src/infrastructure/repositories/gc_log_repository_impl.rs` 작성
  - [x] `insert()` 구현
  - [x] `find_by_date_range()` 구현
- [x] `src/infrastructure/repositories/mod.rs` 업데이트

### Phase 4: Service Layer (완료 ✅)
- [x] `src/application/services/gc_service.rs` 작성
  - [x] `GcService` trait 정의
  - [x] `GcResult` struct 정의
- [x] `src/application/services/gc_service_impl.rs` 작성
  - [x] `timeout_pending_snapshots()` 구현
  - [x] `cleanup_failed_snapshots()` 구현
  - [x] Dry-run 모드 지원
- [x] `src/application/services/mod.rs` 업데이트

### Phase 5: Binary Layer (완료 ✅)
- [x] `src/bin/gc_runner.rs` 작성
  - [x] CLI 구조 정의 (Clap)
  - [x] `timeout-pending` 명령
  - [x] `cleanup-failed` 명령
  - [x] DI 컨테이너 구성
  - [x] 환경 변수 기반 설정
- [x] `Cargo.toml` 업데이트
  - [x] `[[bin]]` 섹션 추가
  - [x] `clap`, `env_logger` 의존성 추가
- [x] 로컬 빌드 및 실행 테스트
- [x] **Shell 테스트**: `./tests/bin/test_gc_runner.sh` (7개 테스트 통과)

### Phase 6: E2E 테스트 ✅ **완료**
- [x] `tests/e2e/test_gc_e2e.py` 작성
  - [x] **시나리오 1**: PENDING 타임아웃 (Dry-run)
  - [x] **시나리오 2**: PENDING 타임아웃 (실제 실행)
  - [x] **시나리오 3**: FAILED 정리 (Dry-run)
  - [x] **시나리오 4**: FAILED 정리 (실제 실행)
  - [x] **시나리오 5**: Grace Period 검증
  - [x] **시나리오 6**: 전체 워크플로우
  - [x] **시나리오 7**: 멱등성 테스트
  - [x] **시나리오 8**: Grace Period 경계값
  - [x] **시나리오 9**: 빈 결과 처리
  - [x] **시나리오 10**: Batch Size 경계값
  - [x] **시나리오 11**: NULL snapshot_image_key
  - [x] **시나리오 12**: Advisory Lock 검증
  - [x] **시나리오 13**: Job A/B 독립성
  - [x] **시나리오 14**: 락 자동 해제
- [x] `tests/e2e/run_e2e_tests.sh` 작성
- [x] **실행**: `./tests/e2e/run_e2e_tests.sh` (14/14 통과)

### Phase 7: 배포 준비 (대기 중 ⏳)
- [ ] `Dockerfile` 수정
  - [ ] `gc_runner` 바이너리 빌드
  - [ ] 바이너리 복사
- [ ] Docker 이미지 빌드 및 푸시
- [ ] `k8s/cronjobs/gc-job-a-timeout-pending.yaml` 작성
- [ ] `k8s/cronjobs/gc-job-b-cleanup-failed.yaml` 작성
- [ ] ConfigMap/Secret 생성
- [ ] CronJob 배포
- [ ] 수동 실행 테스트

### CI/CD 통합
- [ ] `.github/workflows/gc-tests.yml` 작성
- [ ] GitHub Actions 파이프라인 검증
- [ ] 모든 테스트 통과 확인
- [ ] 로그 모니터링

---

## 🧪 테스트 실행 가이드

### 테스트 커버리지

| Layer | 테스트 유형 | 파일 수 | 예상 시간 |
|-------|------------|---------|----------|
| DB Migration | SQL 검증 | 2 | 5분 |
| Domain | 단위 테스트 | 1 | 10분 |
| Repository | 단위 + 통합 | 2 | 30분 |
| Service | 단위 (Mock) + 통합 | 2 | 30분 |
| Binary | CLI + Shell | 3 | 15분 |
| E2E | Python 시나리오 | 1 | 30분 |
| **합계** | | **11** | **2시간** |

### 모든 테스트 실행

```bash
# 1. DB 마이그레이션 테스트
./tests/migrations/test_039_gc_deletion_log.sh

# 2. 단위 테스트 (모든 Layer)
cargo test --lib

# 3. 통합 테스트
cargo test --test gc_integration_test

# 4. Binary 테스트
./tests/bin/test_gc_runner.sh
./tests/bin/test_env_validation.sh

# 5. E2E 테스트
./tests/e2e/run_e2e_tests.sh
```

### 개별 테스트 실행

```bash
# Domain Layer 테스트
cargo test --test gc_deletion_log_test

# Repository Layer 테스트
cargo test --test gc_repository_test
cargo test --test gc_log_repository_test

# Service Layer 테스트
cargo test --test gc_service_test

# 특정 E2E 시나리오만
python3 tests/e2e/test_gc_e2e.py
```

### E2E 테스트 시나리오 (14개)

| 시나리오 | 설명 | 검증 항목 | 상태 |
|---------|------|----------|------|
| 1 | PENDING 타임아웃 (Dry-run) | DB 상태 변경 없음, 로그 기록 | ✅ |
| 2 | PENDING 타임아웃 (실제 실행) | PENDING → FAILED, 로그 기록 | ✅ |
| 3 | FAILED 정리 (Dry-run) | S3 파일 삭제 안됨, 로그 기록 | ✅ |
| 4 | FAILED 정리 (실제 실행) | S3 파일 삭제됨, 로그 기록, file_size 기록 | ✅ |
| 5 | Grace Period 검증 | Grace Period 미만 처리 안됨 | ✅ |
| 6 | 전체 워크플로우 | Job A + Job B 연속 실행 | ✅ |
| 7 | 멱등성 테스트 | 중복 실행 시 안전 처리 | ✅ |
| 8 | Grace Period 경계값 | 정확히 일치하는 경우 처리 | ✅ |
| 9 | 빈 결과 처리 | 처리할 항목 없어도 정상 종료 | ✅ |
| 10 | Batch Size 경계값 | 정확한 배치 크기 준수 | ✅ |
| 11 | NULL snapshot_image_key | NULL 자동 필터링 | ✅ |
| 12 | Advisory Lock 검증 | 동시 실행 방지 | ✅ |
| 13 | Job A/B 독립성 | 서로 다른 락 ID 사용 | ✅ |
| 14 | 락 자동 해제 | 커넥션 종료 시 자동 해제 | ✅ |

---

## 🚀 빠른 시작 가이드

### 1. 로컬 개발 환경 설정
```bash
# 1. DB 마이그레이션
sqlx migrate run

# 2. 빌드
cargo build --bin gc_runner

# 3. Dry-run 테스트
./target/debug/gc_runner timeout-pending --dry-run=true
./target/debug/gc_runner cleanup-failed --dry-run=true

# 4. 실제 실행 (주의!)
./target/debug/gc_runner timeout-pending --grace-days=3 --batch-size=100
./target/debug/gc_runner cleanup-failed --grace-days=7 --batch-size=100
```

### 2. Docker 빌드 및 실행
```bash
# 1. 이미지 빌드
docker build -t pacs-server:gc-test .

# 2. 컨테이너 실행
docker run --rm \
  -e DATABASE_URL=postgresql://... \
  -e S3_BUCKET=... \
  -e S3_REGION=... \
  -e S3_ACCESS_KEY=... \
  -e S3_SECRET_KEY=... \
  pacs-server:gc-test \
  /usr/local/bin/gc_runner timeout-pending --dry-run=true
```

### 3. Kubernetes 배포
```bash
# 1. ConfigMap/Secret 생성
kubectl apply -f k8s/config/pacs-config.yaml
kubectl apply -f k8s/secrets/pacs-secrets.yaml

# 2. CronJob 배포
kubectl apply -f k8s/cronjobs/gc-job-a-timeout-pending.yaml
kubectl apply -f k8s/cronjobs/gc-job-b-cleanup-failed.yaml

# 3. 상태 확인
kubectl get cronjobs -n pacs-system
kubectl get jobs -n pacs-system

# 4. 수동 실행 (테스트)
kubectl create job --from=cronjob/gc-job-a-timeout-pending gc-test-a -n pacs-system

# 5. 로그 확인
kubectl logs -f job/gc-test-a -n pacs-system
```

---

## 📊 모니터링 및 알림

### 1. 로그 모니터링
```bash
# CronJob 실행 로그 확인
kubectl logs -f cronjob/gc-job-a-timeout-pending -n pacs-system

# 최근 실패한 Job 확인
kubectl get jobs -n pacs-system --field-selector status.successful=0

# 특정 Job의 상세 로그
kubectl describe job gc-job-a-timeout-pending-xxxxx -n pacs-system
```

### 2. 메트릭 수집 (Prometheus)
```yaml
# ServiceMonitor 예시
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gc-job-monitor
spec:
  selector:
    matchLabels:
      app: gc-runner
  endpoints:
  - port: metrics
    interval: 30s
```

### 3. 알림 설정 (Alertmanager)
```yaml
# Alert 예시
- alert: GCJobFailed
  expr: kube_job_status_failed{job_name=~"gc-job-.*"} > 0
  for: 5m
  labels:
    severity: warning
  annotations:
    summary: "GC Job {{ $labels.job_name }} failed"
    description: "GC Job has failed. Check logs for details."
```

### 4. 대시보드 (Grafana)
- **패널 1**: GC Job 실행 횟수 (성공/실패)
- **패널 2**: 삭제된 스냅샷 개수
- **패널 3**: 삭제된 파일 크기 (바이트)
- **패널 4**: Job 실행 시간

---

## ⚡ 성능 최적화

### Phase 2: 배치 삭제 구현 (선택사항)

#### 1. ObjectStorageService에 배치 삭제 메서드 추가

**경로**: `pacs-server/src/application/services/object_storage_service.rs`

```rust
#[async_trait]
pub trait ObjectStorageService: Send + Sync {
    // ... 기존 메서드 ...

    /// 여러 파일을 한 번에 삭제 (S3 DeleteObjects API 사용)
    async fn delete_files_batch(
        &self,
        file_paths: Vec<String>,
    ) -> Result<BatchDeleteResult, ObjectStorageError>;
}

#[derive(Debug, Clone)]
pub struct BatchDeleteResult {
    pub deleted: Vec<String>,
    pub errors: Vec<(String, String)>,  // (file_path, error_message)
}
```

#### 2. S3ObjectStorageService에 구현 추가

**경로**: `pacs-server/src/infrastructure/external/s3_object_storage_service.rs`

```rust
use aws_sdk_s3::types::{Delete, ObjectIdentifier};

#[async_trait]
impl ObjectStorageService for S3ObjectStorageService {
    // ... 기존 메서드 ...

    async fn delete_files_batch(
        &self,
        file_paths: Vec<String>,
    ) -> Result<BatchDeleteResult, ObjectStorageError> {
        // S3 DeleteObjects API는 최대 1000개까지 지원
        let objects: Vec<ObjectIdentifier> = file_paths
            .iter()
            .map(|path| {
                ObjectIdentifier::builder()
                    .key(self.generate_object_key(path))
                    .build()
                    .unwrap()
            })
            .collect();

        let delete = Delete::builder()
            .set_objects(Some(objects))
            .build()
            .map_err(|e| ObjectStorageError::S3Error(e.to_string()))?;

        let result = self.client
            .delete_objects()
            .bucket(&self.bucket_name)
            .delete(delete)
            .send()
            .await
            .map_err(|e| ObjectStorageError::S3Error(e.to_string()))?;

        // 결과 파싱
        let deleted = result.deleted()
            .iter()
            .filter_map(|d| d.key().map(|k| k.to_string()))
            .collect();

        let errors = result.errors()
            .iter()
            .filter_map(|e| {
                let key = e.key()?.to_string();
                let message = e.message()?.to_string();
                Some((key, message))
            })
            .collect();

        Ok(BatchDeleteResult { deleted, errors })
    }
}
```

#### 3. GcService에서 배치 삭제 사용

**경로**: `pacs-server/src/application/services/gc_service_impl.rs`

```rust
async fn cleanup_failed_snapshots(
    &self,
    grace_days: i32,
    batch_size: i32,
    dry_run: bool,
) -> Result<Vec<GcResult>, ServiceError> {
    let annotations = self.gc_repository
        .find_failed_snapshots(grace_days, batch_size)
        .await?;

    if dry_run {
        // Dry-run 로직 (기존과 동일)
        // ...
    } else {
        // 배치 삭제 사용
        let file_paths: Vec<String> = annotations
            .iter()
            .filter_map(|a| a.snapshot_image_key.clone())
            .collect();

        let batch_result = self.object_storage
            .delete_files_batch(file_paths)
            .await
            .map_err(|e| ServiceError::ExternalServiceError(e.to_string()))?;

        // 결과 처리 및 로그 기록
        for key in batch_result.deleted {
            // 성공 로그 기록
            // ...
        }

        for (key, error) in batch_result.errors {
            // 실패 로그 기록
            // ...
        }
    }

    Ok(results)
}
```

---

## 🔧 에러 복구 전략

### 1. 재시도 로직 (CronJob 레벨)
```yaml
# CronJob에서 재시도 설정
spec:
  jobTemplate:
    spec:
      backoffLimit: 3  # 최대 3번 재시도
      template:
        spec:
          restartPolicy: OnFailure
```

### 2. 수동 복구 스크립트
```bash
#!/bin/bash
# manual_gc_recovery.sh

# 실패한 로그 조회
psql -U pacs_user -d pacs_db -c "
SELECT annotation_id, snapshot_image_key, error_message
FROM gc_deletion_log
WHERE status = 'failed'
  AND deleted_at > NOW() - INTERVAL '1 day'
ORDER BY deleted_at DESC;
"

# 수동으로 재실행
kubectl create job --from=cronjob/gc-job-b-cleanup-failed gc-manual-recovery -n pacs-system
```

### 3. Dead Letter Queue (DLQ) 패턴
```rust
// 실패한 작업을 별도 테이블에 저장
CREATE TABLE gc_failed_queue (
    id BIGSERIAL PRIMARY KEY,
    annotation_id INTEGER NOT NULL,
    snapshot_image_key TEXT NOT NULL,
    retry_count INTEGER DEFAULT 0,
    last_error TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 📈 성능 벤치마크

### 예상 처리량
| 작업 | 배치 크기 | 예상 시간 | 처리량 |
|------|----------|----------|--------|
| Job A (PENDING → FAILED) | 1000개 | ~30초 | 33개/초 |
| Job B (S3 삭제, 개별) | 1000개 | ~10분 | 1.7개/초 |
| Job B (S3 삭제, 배치) | 1000개 | ~1분 | 16.7개/초 |

### 최적화 효과
- **배치 삭제 사용 시**: 약 **10배** 성능 향상
- **메모리 사용량**: 256MB → 512MB (배치 크기 1000개 기준)
- **네트워크 비용**: S3 API 호출 1000회 → 1회

---

## 🎯 최종 권장 사항

### Phase 1 (MVP)
1. ✅ 개별 삭제 방식으로 구현
2. ✅ Dry-run 모드로 충분히 테스트
3. ✅ 로그 모니터링 설정
4. ✅ 프로덕션 배포

### Phase 2 (최적화)
1. ⏳ 배치 삭제 구현
2. ⏳ 성능 벤치마크
3. ⏳ 메트릭 수집 및 대시보드
4. ⏳ 알림 설정

### Phase 3 (고도화)
1. ⏳ Dead Letter Queue 구현
2. ⏳ 자동 복구 로직
3. ⏳ 비용 최적화 (S3 Lifecycle Policy)
4. ⏳ 멀티 리전 지원

---

## 📚 참고 문서

### 내부 문서
- [01-요구사항-분석.md](./01-요구사항-분석.md)
- [02-DB-스키마-설계.md](./02-DB-스키마-설계.md)
- [03-API-설계.md](./03-API-설계.md)
- [04-에러-처리-및-로깅.md](./04-에러-처리-및-로깅.md)
- [05-배포-및-운영.md](./05-배포-및-운영.md)
- [06-아키텍처-및-바이너리-구조.md](./06-아키텍처-및-바이너리-구조.md)
- [07-모듈-설계-및-다이어그램.md](./07-모듈-설계-및-다이어그램.md)
- [08-기존-코드베이스-분석.md](./08-기존-코드베이스-분석.md)
- [09-상세-구현-가이드.md](./09-상세-구현-가이드.md)

### 외부 문서
- [AWS S3 DeleteObjects API](https://docs.aws.amazon.com/AmazonS3/latest/API/API_DeleteObjects.html)
- [Kubernetes CronJob](https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/)
- [SQLx Documentation](https://docs.rs/sqlx/latest/sqlx/)
- [Clap Documentation](https://docs.rs/clap/latest/clap/)

---

## ✅ 최종 확인 사항

### 구현 완료 체크리스트 (2026-01-13)
- [x] ObjectStorageService 추상화 계층 구현
- [x] GC Service Layer 구현
- [x] Repository Layer 구현
- [x] Binary Layer (CLI) 구현
- [x] Database Migration 실행
- [x] CLI 통합 테스트 통과 (7개 테스트)
- [x] 빌드 성공
- [x] Dry-run 모드 테스트 완료
- [x] 문서화 완료

### 배포 전 체크리스트
- [x] CLI 테스트 통과
- [ ] E2E 테스트 작성 및 실행
- [ ] Dry-run 모드로 프로덕션 데이터 검증
- [ ] Dockerfile 업데이트
- [ ] Kubernetes CronJob YAML 작성
- [ ] 로그 모니터링 설정 완료
- [ ] 알림 설정 완료
- [ ] 롤백 계획 수립

### 배포 후 체크리스트
- [ ] CronJob 정상 실행 확인
- [ ] 로그 정상 기록 확인
- [ ] 메트릭 수집 확인
- [ ] 알림 정상 작동 확인
- [ ] 성능 모니터링 (처리 시간, 메모리 사용량)
- [ ] 비용 모니터링 (S3 API 호출 횟수)

---

## 🎉 구현 완료 요약 (2026-01-13)

### ✅ 완료된 주요 작업

#### 1. ObjectStorageService 추상화 계층
- **Trait 기반 설계**: `ObjectStorageService` trait으로 S3 연동 추상화
- **S3 구현체**: `S3ObjectStorageService`로 실제 S3 연동
- **Factory 패턴**: `ObjectStorageServiceFactory`로 인스턴스 생성
- **에러 처리**: `ObjectStorageError`로 일관된 에러 처리

#### 2. GC Service Layer
- **Job A**: PENDING 상태 타임아웃 처리 (3일 이상 → FAILED)
- **Job B**: FAILED 상태 스냅샷 정리 (7일 이상 → S3 삭제)
- **Dry-run 모드**: 실제 삭제 없이 로그만 기록
- **배치 처리**: 대량 삭제 방지를 위한 배치 크기 제한

#### 3. Repository Layer
- **GcRepository**: annotation 조회 및 상태 업데이트
- **GcLogRepository**: GC 로그 기록 및 조회
- **SQL 쿼리 최적화**: 인덱스 활용 및 효율적인 쿼리

#### 4. Binary Layer (CLI)
- **clap 기반 CLI**: 직관적인 명령어 인터페이스
- **환경 변수 설정**: DATABASE_URL, S3_* 환경 변수
- **상세한 도움말**: 한글 도움말 및 예시
- **실행 결과 출력**: 처리 건수 및 성공/실패 통계

#### 5. Database
- **snapshot 컬럼**: `snapshot_image_key`, `snapshot_status`, `snapshot_uploaded_at`
- **GC 로그 테이블**: `gc_deletion_log` 테이블
- **ENUM 타입**: `snapshot_upload_status` (pending/uploading/completed/failed)
- **인덱스**: 성능 최적화를 위한 인덱스

### 📊 테스트 결과

```
✅ CLI 통합 테스트: 7/7 통과
✅ 빌드 테스트: 성공
✅ 데이터베이스 마이그레이션: 성공
✅ Dry-run 모드: 정상 작동
```

### 🚀 실행 방법

#### Job A: PENDING 타임아웃 처리
```bash
DATABASE_URL="postgresql://user@localhost:5432/pacs_db" \
S3_BUCKET="pacs-snapshots" \
S3_REGION="ap-northeast-2" \
S3_ACCESS_KEY="xxx" \
S3_SECRET_KEY="xxx" \
./target/debug/gc_runner timeout-pending --grace-days 3 --batch-size 1000 --dry-run
```

#### Job B: FAILED 스냅샷 정리
```bash
./target/debug/gc_runner cleanup-failed --grace-days 7 --batch-size 1000 --dry-run
```

### 📝 다음 단계

1. **E2E 테스트 작성** - 실제 데이터로 전체 워크플로우 테스트
2. **Dockerfile 업데이트** - `gc_runner` 바이너리 포함
3. **Kubernetes CronJob 작성** - Job A/B CronJob YAML
4. **프로덕션 배포** - Dry-run 모드로 검증 후 실제 배포
5. **모니터링 설정** - 로그 및 메트릭 수집

---

**GC Batch Job 구현 완료! 🎉**

**다음 단계**: [09-상세-구현-가이드.md](./09-상세-구현-가이드.md)를 따라 코드 작성을 시작하세요.

