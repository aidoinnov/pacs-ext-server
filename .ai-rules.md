# AI Assistant Rules for PACS-EXT-SERVER

> **중요**: AI 어시스턴트는 세션 시작 시 이 파일을 반드시 읽고 규칙을 따라야 합니다.

## 📁 프로젝트 구조

```
pacs-ext-server/
├── pacs-server/           # 메인 서버 (FastAPI)
│   ├── e2e/              # E2E 테스트 (중요!)
│   │   ├── test_base.py          # 테스트 베이스 클래스
│   │   ├── test_fixtures.py      # 테스트 데이터 생성
│   │   ├── test_utils.py         # 유틸리티 함수
│   │   └── E2E_TEST_RULES.md     # ⭐ E2E 테스트 작성 규칙
│   ├── app/              # 애플리케이션 코드
│   └── tests/            # 유닛 테스트
└── .ai-rules.md          # 이 파일
```

---

## 🎯 E2E 테스트 작성 규칙

### ⚠️ 필수 확인 사항

E2E 테스트 작성 또는 수정 시 **반드시** 다음 파일을 먼저 읽을 것:
- **`pacs-server/e2e/E2E_TEST_RULES.md`** ← 가장 중요!

### 핵심 규칙 요약

1. **항상 `BaseE2ETest` 상속**
   ```python
   from test_base import BaseE2ETest
   
   class MyTest(BaseE2ETest):
       def get_test_name(self) -> str:
           return "테스트 이름"
       
       def run_tests(self):
           # 테스트 로직
           pass
   ```

2. **자동 클린업 사용**
   ```python
   # 리소스 생성 시 리스트에 추가만 하면 자동 삭제됨
   ann_id = create_annotation(...)
   self.created_annotation_ids.append(ann_id)  # ⭐ 자동 삭제!
   ```

3. **공통 모듈 활용**
   - `TestConfig`: 설정 (URL, 인증, DICOM UIDs)
   - `TestAuth`: 로그인
   - `TestPrinter`: 출력 포맷
   - `AnnotationFixtures`: 어노테이션 생성
   - `UserFixtures`: 사용자 생성
   - `ImageFixtures`: 이미지 생성

4. **금지 사항**
   - ❌ 독립 스크립트 작성
   - ❌ 설정 하드코딩
   - ❌ 수동 cleanup 호출
   - ❌ 중복 로그인 코드

---

## 🔧 개발 환경

### Python 버전
- Python 3.13

### 주요 의존성
- FastAPI
- SQLAlchemy
- Pillow (이미지 처리)
- requests (E2E 테스트)

### 서버 실행
```bash
cd pacs-server
uvicorn app.main:app --reload --port 8080
```

### E2E 테스트 실행
```bash
cd pacs-server/e2e
python test_annotation_level_filtering_refactored.py
```

---

## 📋 코딩 컨벤션

### 1. 파일명
- E2E 테스트: `test_<기능>_e2e.py` 또는 `test_<기능>_refactored.py`
- 유닛 테스트: `test_<모듈>.py`
- 픽스처: `test_fixtures.py`
- 유틸리티: `test_utils.py`

### 2. 클래스명
- E2E 테스트: `<기능>E2ETest` (예: `AnnotationLevelFilteringTest`)
- 픽스처: `<리소스>Fixtures` (예: `AnnotationFixtures`)

### 3. 함수명
- 테스트 메서드: `test_<기능>()`
- 헬퍼 메서드: `_<동작>()` (private)
- 픽스처: `create_<리소스>()`

### 4. 주석
- 한글 주석 사용 (프로젝트 표준)
- Docstring은 간결하게

---

## 🚀 새 E2E 테스트 작성 프로세스

### 1. 규칙 확인
```bash
# 반드시 먼저 읽기!
cat pacs-server/e2e/E2E_TEST_RULES.md
```

### 2. 템플릿 사용
```python
#!/usr/bin/env python3
"""테스트 설명"""

from test_base import BaseE2ETest, TestConfig, TestPrinter
from test_fixtures import AnnotationFixtures

class MyE2ETest(BaseE2ETest):
    def get_test_name(self) -> str:
        return "내 테스트"
    
    def run_tests(self):
        # 테스트 로직
        pass

if __name__ == '__main__':
    test = MyE2ETest()
    test.run()
```

### 3. 체크리스트
- [ ] `BaseE2ETest` 상속
- [ ] `get_test_name()` 구현
- [ ] `run_tests()` 구현
- [ ] 리소스를 `self.created_*_ids`에 추가
- [ ] `TestConfig` 사용 (하드코딩 금지)
- [ ] `TestPrinter` 사용
- [ ] 픽스처 활용

---

## 📚 참고 문서 우선순위

E2E 테스트 관련 작업 시 읽어야 할 순서:

1. **`pacs-server/e2e/CONNECTION_INFO.md`** - 연결 정보 및 설정
2. **`pacs-server/e2e/E2E_TEST_RULES.md`** ⭐ 가장 중요!
3. `pacs-server/e2e/REFACTORING_GUIDE.md` - 상세 가이드
4. `pacs-server/e2e/CLEANUP_VERIFICATION.md` - 클린업 검증
5. `pacs-server/e2e/test_base.py` - 소스 코드
6. `pacs-server/e2e/test_fixtures.py` - 소스 코드

---

## 🎯 AI 어시스턴트 행동 규칙

### E2E 테스트 작성/수정 시

1. **먼저 `E2E_TEST_RULES.md` 읽기**
   ```
   사용자: "새 E2E 테스트 만들어줘"
   AI: (먼저 E2E_TEST_RULES.md 읽음)
       "네, E2E_TEST_RULES.md를 확인했습니다. 
        BaseE2ETest를 상속받아 작성하겠습니다..."
   ```

2. **규칙 준수 확인**
   - BaseE2ETest 상속 여부
   - 자동 클린업 사용 여부
   - 공통 모듈 활용 여부
   - 금지 사항 위반 여부

3. **기존 테스트 참고**
   - `test_annotation_level_filtering_refactored.py` (가장 깔끔)
   - `test_annotation_head_request_refactored.py`
   - `test_annotation_version_conflict_refactored.py`

### 코드 리뷰 시

1. **리팩토링 여부 확인**
   - 기존 방식(`*_refactored.py` 없음) → 리팩토링 제안
   - 리팩토링 방식 → 규칙 준수 확인

2. **중복 코드 확인**
   - 로그인 코드 중복 → `TestAuth` 사용 제안
   - 설정 하드코딩 → `TestConfig` 사용 제안
   - 어노테이션 생성 중복 → `AnnotationFixtures` 사용 제안

---

## 💡 자주 하는 실수

### ❌ 실수 1: 독립 스크립트 작성
```python
# 잘못된 예
if __name__ == '__main__':
    token = login()
    try:
        # 테스트...
    finally:
        cleanup_annotations(token, created_ids)
```

### ✅ 올바른 방법
```python
class MyTest(BaseE2ETest):
    def run_tests(self):
        ann_id = create_annotation(...)
        self.created_annotation_ids.append(ann_id)

if __name__ == '__main__':
    test = MyTest()
    test.run()  # 자동 setup/teardown
```

### ❌ 실수 2: 설정 하드코딩
```python
# 잘못된 예
BASE_URL = "http://localhost:8080"
STUDY_UID = "1.2.840.113619..."
```

### ✅ 올바른 방법
```python
from test_base import TestConfig

url = TestConfig.BASE_URL
study_uid = TestConfig.DEFAULT_STUDY_UID
```

---

## 🔄 세션 시작 시 체크리스트

AI 어시스턴트는 새 세션 시작 시:

1. [ ] 이 파일(`.ai-rules.md`) 읽기
2. [ ] E2E 테스트 작업이면 `E2E_TEST_RULES.md` 읽기
3. [ ] Annotation API 작업이면 `issues/2026-01-14-annotation-조회-삭제-오류-수정/CONTEXT-FOR-AI.md` 읽기
4. [ ] 프로젝트 구조 파악
5. [ ] 최근 변경사항 확인 (git log)

---

## � Annotation Snapshot API

> **최근 구현**: 2026-01-14
> **상세 문서**: `issues/2026-01-14-annotation-조회-삭제-오류-수정/`

### 개요

Annotation에 스냅샷 이미지를 업로드/조회/삭제할 수 있는 API가 구현되었습니다.

### DB 스키마

`annotation_annotation` 테이블에 다음 컬럼이 추가되었습니다:

```sql
-- Migration 036, 037
snapshot_image_key VARCHAR(512)              -- S3 키
snapshot_status VARCHAR(50)                  -- 상태 (pending, uploaded, failed)
snapshot_uploaded_at TIMESTAMP WITH TIME ZONE -- 업로드 시각
```

### Entity 구조체

```rust
// pacs-server/src/domain/entities/annotation.rs
pub struct Annotation {
    // ... 기존 필드들
    pub snapshot_image_key: Option<String>,
    pub snapshot_status: Option<SnapshotUploadStatus>,
    pub snapshot_uploaded_at: Option<DateTime<Utc>>,
    // ... 나머지 필드들
}

pub enum SnapshotUploadStatus {
    Pending,    // 업로드 대기 중
    Uploaded,   // 업로드 완료
    Failed,     // 업로드 실패
}
```

### API 엔드포인트

#### 1. 업로드
```http
POST /api/v1/annotations/{annotation_id}/snapshot
Content-Type: multipart/form-data

- 최대 크기: 10MB
- 지원 형식: PNG, JPEG, WebP
- 권한: Annotation 소유자만
```

#### 2. 조회
```http
GET /api/v1/annotations/{annotation_id}/snapshot

- 응답: 이미지 바이너리 데이터
- 권한: Annotation 조회 권한이 있는 사용자
```

#### 3. 삭제
```http
DELETE /api/v1/annotations/{annotation_id}/snapshot

- 응답: 204 No Content
- 권한: Annotation 소유자만
```

### Python 사용 예시

```python
import requests

# 업로드
with open('snapshot.png', 'rb') as f:
    response = requests.post(
        f'{BASE_URL}/api/v1/annotations/{ann_id}/snapshot',
        headers={'Authorization': f'Bearer {token}'},
        files={'file': ('snapshot.png', f, 'image/png')}
    )

# 조회
response = requests.get(
    f'{BASE_URL}/api/v1/annotations/{ann_id}/snapshot',
    headers={'Authorization': f'Bearer {token}'}
)

# 삭제
response = requests.delete(
    f'{BASE_URL}/api/v1/annotations/{ann_id}/snapshot',
    headers={'Authorization': f'Bearer {token}'}
)
```

### E2E 테스트

```bash
cd pacs-server/e2e
python test_annotation_snapshot_e2e_refactored.py
```

### 중요 사항

⚠️ **Entity 변경 시 주의사항**:
- Entity에 필드를 추가할 때는 **반드시** Repository의 모든 SELECT 쿼리를 업데이트해야 합니다!
- 특히 `annotation_repository_impl.rs`의 모든 쿼리에 새 필드를 추가해야 합니다.
- 자세한 내용은 `issues/2026-01-14-annotation-조회-삭제-오류-수정/체크리스트.md` 참고

### 관련 파일

- **Entity**: `pacs-server/src/domain/entities/annotation.rs`
- **Repository**: `pacs-server/src/infrastructure/repositories/annotation_repository_impl.rs`
- **Migration**: `pacs-server/migrations/036_*.sql`, `037_*.sql`
- **E2E 테스트**: `pacs-server/e2e/test_annotation_snapshot_e2e_refactored.py`
- **상세 문서**: `issues/2026-01-14-annotation-조회-삭제-오류-수정/CONTEXT-FOR-AI.md`

---

## �📞 도움말

- 연결 정보: `pacs-server/e2e/CONNECTION_INFO.md`
- E2E 테스트 규칙: `pacs-server/e2e/E2E_TEST_RULES.md`
- 리팩토링 가이드: `pacs-server/e2e/REFACTORING_GUIDE.md`
- 클린업 검증: `pacs-server/e2e/CLEANUP_VERIFICATION.md`
- **Annotation Snapshot API**: `issues/2026-01-14-annotation-조회-삭제-오류-수정/API-구현-안내.md`
- **Entity 변경 체크리스트**: `issues/2026-01-14-annotation-조회-삭제-오류-수정/체크리스트.md`

